<% headers = ["Id", "Username", "Age", "Gender"] %>
<%= CSV.generate_line headers, {:col_sep => ';'} %>
<% @users.each do |user| %>
	<% row = [ user.id,
				     user.username,
				     user.age,
				     user.gender] %>
	<%= CSV.generate_line row, {:row_sep => nil, :col_sep => ';'} %>
<% end %>
